// Backend removed: this project is frontend-only.
// The original server code was intentionally removed to keep the repository focused on the
// client-side React application. This placeholder file prevents accidental imports or
// tooling that expects a TypeScript file at this path.

// If you later decide to reintroduce a server, recreate the appropriate server files
// under this directory. For now, delete this file if you want the directory removed.
