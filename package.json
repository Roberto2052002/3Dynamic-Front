{
  "name": "workspace-root",
  "private": true,
  "scripts": {
    "dev": "cd RecipeFinder && npm run dev",
    "build": "cd RecipeFinder && npm run build",
    "check": "cd RecipeFinder && npm run check",
    "client:dev": "cd RecipeFinder && npm run dev"
  },
  "dependencies": {
    "react-router-dom": "^7.8.2"
  }
}
